name: Release (Windows)

# Builds the portable Windows .exe and attaches it to a GitHub release, so the
# app can be downloaded on any machine. Push a version tag to start it:
#
#   git tag v0.1.1 && git push origin v0.1.1
#
# You can also start it by hand from the Actions tab. A hand-started run builds
# `source_ref` and creates the tag itself, so the tag does not have to exist
# first. Point `source_ref` at the tag to rebuild a release that already exists.
#
# A third way needs no tag and no Actions tab: push a branch named after the
# version, for example `release/v0.1.3`. The run builds that branch and creates
# the tag on it. This is the route for anyone who can push a branch but not a
# tag. Delete the branch afterwards. The tag and the release stay.
on:
  push:
    tags:
      - 'v*'
    branches:
      - 'release/v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag to publish, for example v0.1.3. It is created if it does not exist.'
        required: true
      source_ref:
        description: 'Branch, tag or commit to build and tag'
        required: false
        default: main

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.source_ref || github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # One name for the version, whichever of the three routes started the run:
      # the input, the pushed tag, or the release branch minus its prefix.
      - name: Work out the version this run publishes
        shell: bash
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
          REF_NAME: ${{ github.ref_name }}
        run: echo "RELEASE_TAG=${INPUT_TAG:-${REF_NAME#release/}}" >> "$GITHUB_ENV"

      - name: Check release version
        run: node -e "const v = require('./package.json').version; if (process.env.RELEASE_TAG !== 'v' + v) throw new Error('Tag and package version must match');"

      - run: npm run check
      - run: npm run format:check
      - run: node --test electron/security.test.cjs

      # Pulls the piano samples into public/samples, so the installed app needs
      # no network.
      - name: Fetch piano samples
        run: npm run samples

      - run: npm run build
      - run: npm run check:bundle

      # Only the portable target. The NSIS installer would double the build time.
      - name: Package portable .exe
        run: npx electron-builder --win portable --publish never

      - name: Publish to the release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG="$RELEASE_TAG"
          gh release view "$TAG" >/dev/null 2>&1 || gh release create "$TAG" --target "$GITHUB_SHA" --title "Keysense ${TAG#v}" --generate-notes
          gh release upload "$TAG" release/*-portable.exe --clobber
